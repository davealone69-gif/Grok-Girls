import {Girl,Room} from '../models/studio';
import {chatWithProvider,ProviderName} from './providers';
export interface ChatMessage{id:string;role:'user'|'assistant';text:string;createdAt:number}
const KEY='grok-girls-chat-v1';
export function loadChat(id:string):ChatMessage[]{try{return JSON.parse(localStorage.getItem(`${KEY}:${id}`)||'[]') as ChatMessage[]}catch{return[]}}
export function saveChat(id:string,messages:ChatMessage[]){localStorage.setItem(`${KEY}:${id}`,JSON.stringify(messages.slice(-200)))}
export function localReply(girl:Girl,room:Room,message:string){const m=message.toLowerCase();if(m.includes('hello')||m.includes('hi')||m.includes('hey'))return `${girl.name}: Good to see you. We're in the ${room.name}. What are we creating?`;if(m.includes('room'))return `${girl.name}: ${room.name} has ${room.lighting.toLowerCase()}.`;if(m.includes('remember'))return girl.memories.length?`${girl.name}: I have ${girl.memories.length} saved memories.`:`${girl.name}: Nothing saved yet.`;return `${girl.name}: I'm in local mode. I heard you and can keep this conversation in memory.`}
export async function reply(girl:Girl,room:Room,history:ChatMessage[],message:string,provider:ProviderName){const system=`You are ${girl.name}, an adult fictional companion. Personality: ${girl.traits.join(', ')}. Bio: ${girl.bio}. Current room: ${room.name}. Be warm, conversational and consistent with the character.`;if(provider==='local')return localReply(girl,room,message);const response=await chatWithProvider([{role:'system',content:system},...history.slice(-20).map(m=>({role:m.role,content:m.text})),{role:'user',content:message}],provider);return response.text}
