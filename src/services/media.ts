import {GalleryItem,saveGallery} from './gallery';
export function exportGallery(items:GalleryItem[]){const blob=new Blob([JSON.stringify(items,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`grok-girls-gallery-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(url)}
export function importGallery(file:File):Promise<GalleryItem[]>{return new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>{try{const data=JSON.parse(String(r.result));if(!Array.isArray(data))throw new Error('Gallery file must contain an array');saveGallery(data);resolve(data)}catch(e){reject(e)}};r.onerror=()=>reject(r.error);r.readAsText(file)})}
export async function downloadMedia(url:string,filename:string){const r=await fetch(url);if(!r.ok)throw new Error(`Download HTTP ${r.status}`);const blob=await r.blob();const objectUrl=URL.createObjectURL(blob);const a=document.createElement('a');a.href=objectUrl;a.download=filename;a.click();URL.revokeObjectURL(objectUrl)}
