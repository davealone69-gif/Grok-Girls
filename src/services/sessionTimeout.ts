export function withTimeout<T>(promise:Promise<T>,ms:number,signal?:AbortSignal):Promise<T>{return new Promise((resolve,reject)=>{let done=false;const finish=(fn:(v:any)=>void,v:any)=>{if(done)return;done=true;clearTimeout(timer);signal?.removeEventListener('abort',abort);fn(v)};const timer=setTimeout(()=>finish(reject,new Error('Session timeout')),Math.max(1,ms));const abort=()=>finish(reject,new DOMException('Aborted','AbortError'));if(signal?.aborted)return abort();signal?.addEventListener('abort',abort,{once:true});promise.then(v=>finish(resolve,v),e=>finish(reject,e));});}
