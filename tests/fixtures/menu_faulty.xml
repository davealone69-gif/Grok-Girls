<?xml version="1.0" encoding="UTF-8"?>
<!-- DELIBERATELY FAULTY MENU — unclosed label attribute, unclosed item
     and section tags, plus an item missing its required id. Used by the
     audit suite to prove the app falls back to the built-in menu. -->
<menu version="1">
  <section id="rail">
    <item id="presets" label="BROKEN PRESETS
    <item icon="🗂️" title="Missing id attribute">
    <item id="gallery" title="Generation Archive">
  </section>
  <section id="dock">
    <item id="hair_style" label="HAIR STYLE"
  </section>
</menu>
